#include<stdio.h>
int main()
{
    float r,A;
    printf("Enter the value if Radius for Area of circle:");
    scanf("%f",&r);
    A=3.14*r*r;
    printf("The area of circle is %f",A);
    return 0;
}
